import { generalFunc, gcdFunc } from '../index.js';

const brainGcd = () => {
  generalFunc();
  console.log('Find the greatest common divisor of given numbers.');
  gcdFunc();
};

export default brainGcd;
