import { generalFunc, progressionFunc } from '../index.js';

const brainProgression = () => {
  generalFunc();
  console.log('What number is missing in the progression?');
  progressionFunc();
};

export default brainProgression;
