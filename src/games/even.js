import { generalFunc, evenFunc } from '../index.js';

const brainEven = () => {
  generalFunc();
  console.log('Answer "yes" if the number is even, otherwise answer "no".');
  evenFunc();
};
export default brainEven;
